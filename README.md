# stratum
Modified / Fixed Stratum for Kryptofranc and Yiimp

This stratum is used with the Yiimp Pool of Kryptofranc, it fixes several bugs from the original code that was blocking the Pool, either these bugs were left intentionally or they are from sloppy programmers, Yiimp cannot work correctly otherwise.

